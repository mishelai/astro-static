---
// Premium About Section
---

<section class="about-section section">
  <div class="container">
    <div class="about-grid">
      <div class="about-image">
        <div class="image-wrapper">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 500 380" class="placeholder-svg">
            <rect width="500" height="380" fill="#1e3a8a" rx="20"/>
            <!-- Pipe illustration -->
            <g transform="translate(50, 60)">
              <rect x="0" y="100" width="400" height="60" rx="8" fill="#3b82f6" opacity="0.4"/>
              <rect x="50" y="80" width="80" height="100" rx="8" fill="#3b82f6" opacity="0.5"/>
              <rect x="280" y="80" width="80" height="100" rx="8" fill="#3b82f6" opacity="0.5"/>
              <!-- Wrench -->
              <g transform="translate(150, 100)">
                <rect x="0" y="20" width="100" height="20" rx="4" fill="#60a5fa"/>
                <circle cx="90" cy="30" r="25" fill="none" stroke="#60a5fa" stroke-width="12"/>
              </g>
            </g>
            <!-- Worker silhouette -->
            <g transform="translate(180, 200)">
              <circle cx="70" cy="30" r="30" fill="#60a5fa" opacity="0.6"/>
              <rect x="40" y="65" width="60" height="80" rx="8" fill="#60a5fa" opacity="0.6"/>
            </g>
            <rect x="20" y="340" width="200" height="30" rx="8" fill="rgba(255,255,255,0.1)"/>
            <text x="40" y="362" fill="rgba(255,255,255,0.7)" font-size="14" font-weight="500">Learn more about us</text>
          </svg>
        </div>
      </div>

      <div class="about-content">
        <h2 class="about-title">
          <span class="title-bold">Plumbing solutions</span><br />
          <span class="title-light">tailored</span>
        </h2>
        <p class="about-text">
          providing prompt, professional, and courteous service ensuring that
          your plumbing system is always in optimal condition.
        </p>

        <div class="about-features">
          <div class="feature">
            <div class="feature-dot"></div>
            <span>Experienced and certified plumbers</span>
          </div>
          <div class="feature">
            <div class="feature-dot"></div>
            <span>High-quality materials and equipment</span>
          </div>
          <div class="feature">
            <div class="feature-dot"></div>
            <span>Customer satisfaction guarantee</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    background: var(--white);
    padding: 100px 0;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  .image-wrapper {
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-xl);
  }

  .placeholder-svg {
    width: 100%;
    height: auto;
    display: block;
  }

  .about-title {
    font-size: clamp(2rem, 4vw, 2.75rem);
    line-height: 1.15;
    margin-bottom: 24px;
  }

  .title-bold {
    font-weight: 700;
    color: var(--dark);
  }

  .title-light {
    font-weight: 400;
    color: var(--gray-400);
  }

  .about-text {
    font-size: 1.05rem;
    color: var(--gray-500);
    line-height: 1.7;
    margin-bottom: 32px;
  }

  .about-features {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .feature {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .feature-dot {
    width: 8px;
    height: 8px;
    background: var(--primary);
    border-radius: 50%;
    flex-shrink: 0;
  }

  .feature span {
    font-size: 1rem;
    color: var(--dark);
    font-weight: 500;
  }

  @media (max-width: 900px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }

    .about-content {
      text-align: center;
    }

    .feature {
      justify-content: center;
    }

    .image-wrapper {
      max-width: 500px;
      margin: 0 auto;
    }
  }
</style>
